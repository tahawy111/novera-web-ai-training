<!DOCTYPE html>
<html lang="ar" dir="rtl" class="theme-ocean-night"> <!-- Or dynamically set theme -->

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>معرض الصور - Novera</title>
  <link rel="icon" href="/logo.png">
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Alexandria:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
  <style>
    body {
      font-family: 'Alexandria', sans-serif;
      background-color: #001219;
      color: #e9f5f9;
      padding-top: 80px;
    }

    .container {
      margin: 0 auto;
      padding: 1rem 2rem;
      max-width: 1200px;
    }

    .gallery-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
      gap: 1.5rem;
      padding: 1rem;
    }

    .gallery-item {
      background-color: rgba(0, 95, 115, 0.3);
      /* --card-bg */
      border: 1px solid #005f73;
      /* --border-color */
      border-radius: 0.75rem;
      padding: 1rem;
      text-align: center;
      box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
      /* Animation */
      opacity: 0;
      transform: translateY(20px);
      animation: fadeIn 0.5s ease-out forwards;
      transition: transform 0.3s ease, box-shadow 0.3s ease; /* Add transition for hover effect */
      /* Add transition for hover effect */
    }

    .gallery-item:hover {
      transform: translateY(0) scale(1.3); /* Scale up slightly on hover */
      box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.2), 0 4px 6px -2px rgba(0, 0, 0, 0.1); /* Enhance shadow on hover */
    }

    /* Delay animation for staggered effect */
    .gallery-grid .gallery-item:nth-child(1) {
      animation-delay: 0.1s;
    }

    .gallery-grid .gallery-item:nth-child(2) {
      animation-delay: 0.2s;
    }

    .gallery-grid .gallery-item:nth-child(3) {
      animation-delay: 0.3s;
    }

    .gallery-grid .gallery-item:nth-child(4) {
      animation-delay: 0.4s;
    }

    .gallery-grid .gallery-item:nth-child(5) {
      animation-delay: 0.5s;
    }

    .gallery-grid .gallery-item:nth-child(6) {
      animation-delay: 0.6s;
    }

    /* Add more nth-child rules or use JavaScript for dynamic delay */

    @keyframes fadeIn {
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .gallery-item img {
      width: 100%;
      height: 200px;
      object-fit: cover;
      border-radius: 0.5rem;
      margin-bottom: 0.75rem;
    }

    .gallery-item p {
      color: #94d2bd;
      /* --text-secondary */
      font-size: 0.9rem;
    }

    .gallery-item .type {
      color: #e9f5f9;
      /* --text-primary */
      font-weight: bold;
    }

    .nav-links {
      margin-bottom: 2rem;
      text-align: center;
    }

    .nav-links a {
      margin: 0 10px;
      color: #94d2bd;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
    }

    .nav-links a i {
      margin-left: 5px;
    }

    /* Space between text and icon */
    .nav-links a:hover,
    .nav-links a.active {
      color: #00b4d8;
      /* --accent */
    }

    /* أنماط للتصنيفات المختلفة */
    details {
      background: rgba(0, 95, 115, 0.1);
      border-radius: 1rem;
      padding: 1rem;
      transition: all 0.3s ease;
      margin-bottom: 1.5rem;
      /* Added margin */
    }

    details[data-type="paper"] {
      background: rgba(148, 210, 189, 0.1);
      border-left: 4px solid #94d2bd;
    }

    details[data-type="plastic"] {
      background: rgba(0, 180, 216, 0.1);
      border-left: 4px solid #00b4d8;
    }

    details[data-type="metal"] {
      background: rgba(202, 240, 248, 0.1);
      border-left: 4px solid #caf0f8;
    }

    details[data-type="mixed"] {
      background: rgba(0, 150, 199, 0.1);
      border-left: 4px solid #0096c7;
    }

    details[data-type="unknown"] {
      /* Style for unknown type */
      background: rgba(108, 117, 125, 0.1);
      border-left: 4px solid #6c757d;
    }

    details summary {
      padding: 0.5rem 1rem;
      border-radius: 0.5rem;
      transition: all 0.3s ease;
      cursor: pointer;
      display: flex;
      /* Use flexbox for icon alignment */
      align-items: center;
      transition: background-color 0.3s ease;
      /* Add transition for hover effect */
    }

    details summary i {
      margin-left: 10px;
      /* Space between icon and text */
    }

    details summary:hover {
      background: rgba(255, 255, 255, 0.1);
    }

    /* Animation for details content */
    details[open] .gallery-grid {
      animation: slideDown 0.5s ease-out;
    }

    @keyframes slideDown {
      from {
        opacity: 0;
        transform: translateY(-10px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Animation for the "no images" link */
    @keyframes pulse {

      0%,
      100% {
        opacity: 1;
      }

      50% {
        opacity: 0.8;
      }
    }

    .no-images-link {
      animation: pulse 2s infinite ease-in-out;
    }
  </style>
</head>

<body>
  <header
    style="position: fixed; top: 0; left: 0; right: 0; background-color: rgba(0, 18, 25, 0.9); padding: 1rem; z-index: 50;">
    <div
      style="display: flex; align-items: center; justify-content: space-between; max-width: 1200px; margin: 0 auto; padding: 0 1rem;">
      <img src="/logo.png" alt="Novera Logo" style="height: 40px; width: auto;">
      <nav class="nav-links" style="margin: 0;">
        <a href="/upload" class="<%= currentPage === 'upload' ? 'active' : '' %>">
          <i class="fas fa-upload"></i> رفع صور
        </a>
        <a href="/gallery" class="<%= currentPage === 'gallery' ? 'active' : '' %>">
          <i class="fas fa-images"></i> المعرض
        </a>
      </nav>
    </div>
  </header>

  <div class="container">
    <h1 class="text-3xl font-bold mb-8 text-center" style="color: #e9f5f9;">معرض الصور المصنفة</h1>

    <% if (images && images.length> 0) { %>
      <% const groupedImages=images.reduce((acc, image)=> {
        const type = image.wasteType || 'unknown'; // Handle potential missing type
        if (!acc[type]) {
        acc[type] = [];
        }
        acc[type].push(image);
        return acc;
        }, {});

        const typeNames = {
        'paper': 'ورق',
        'plastic': 'بلاستيك',
        'metal': 'معدن',
        'mixed': 'مختلط',
        'unknown': 'غير مصنف' // Handle unknown type
        };

        const typeIcons = { // Map types to Font Awesome icons
        'paper': 'fas fa-file-alt',
        'plastic': 'fas fa-bottle-water',
        'metal': 'fas fa-wrench',
        'mixed': 'fas fa-boxes-stacked',
        'unknown': 'fas fa-question-circle'
        };
        %>

        <% Object.keys(groupedImages).forEach(type=> { %>
          <details class="mb-8" data-type="<%= type %>">
            <summary class="text-xl font-bold mb-4 cursor-pointer" style="color: #e9f5f9;">
              <i class="<%= typeIcons[type] || typeIcons['unknown'] %>"></i>
              <%= typeNames[type] || type %>
                <span class="text-sm opacity-70 mr-2">(<%= groupedImages[type].length %> صور)</span>
            </summary>
            <div class="gallery-grid mt-4">
              <% groupedImages[type].forEach(image=> { %>
                <div class="gallery-item">
                  <img src="<%= image.imgurUrl %>" alt="Uploaded Waste Image - <%= image.wasteType %>">
                  <p>النوع: <span class="type">
                      <i class="<%= typeIcons[image.wasteType] || typeIcons['unknown'] %> text-base"></i>
                      <%= typeNames[image.wasteType] || image.wasteType %>
                    </span></p>
                  <% if (image.uploaderName) { %> <!-- Display uploader name if available -->
                    <p style="font-size: 0.8rem; opacity: 0.7;">
                      <i class="fas fa-user text-sm"></i>
                      الرافع: <span class="type"><%= image.uploaderName %></span>
                    </p>
                    <% } %>
                      <p style="font-size: 0.8rem; opacity: 0.7;">
                        <i class="fas fa-calendar-alt text-sm"></i>
                        تاريخ الرفع: <%= (new Date(image.uploadedAt)).toLocaleString('ar-EG') %>
                      </p>
                </div>
                <% }); %>
            </div>
          </details>
          <% }); %>
            <% } else { %>
              <p class="text-center text-xl" style="color: #94d2bd;">
                لم يتم رفع أي صور بعد.
                <a href="/upload" style="color: #00b4d8;" class="inline-flex items-center no-images-link">
                  <i class="fas fa-arrow-right ml-2"></i> قم برفع بعض الصور الآن!
                </a>
              </p>
              <% } %>
  </div>
</body>

</html>